{{#if defaultState }}
import { fromJS } from 'immutable';
{{/if}}
import { reducers{{#if defaultState }}, {{ camelCase domain }}Reducer{{/if}} } from '../reducer';

describe('{{ camelCase domain }}Reducer', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });

  it('Expect to have the combined reducers', () => {
    {{#if defaultState }}
    const expected = { global: {{ camelCase domain }}Reducer };
    {{else}}
    const expected = {};
    {{/if}}

    expect(reducers).toEqual(expected);
  });
  {{#if defaultState }}

  it('returns the initial state', () => {
    const expected = fromJS({
      example: '{{ properCase domain }} domain'
    });

    expect({{ camelCase domain }}Reducer(undefined, {})).toEqual(expected); // eslint-disable-line no-undefined
  });
  {{/if}}
});
